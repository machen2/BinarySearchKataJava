public class BinarySearch {
    public static int search(int num, int[] arr) {
        if (arr.length == 0) {
            return -1;
        }

        if (num == arr[0]) {
            return 0;
        }

        return -1;
    }
}
